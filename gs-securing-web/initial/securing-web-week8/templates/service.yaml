apiVersion: v1
kind: Service
metadata:
  name: {{ include "securing-web-week8.fullname" . }}
  labels:
    {{- include "securing-web-week8.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "securing-web-week8.selectorLabels" . | nindent 4 }}
