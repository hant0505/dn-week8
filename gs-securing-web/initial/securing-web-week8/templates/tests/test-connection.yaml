apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "securing-web-week8.fullname" . }}-test-connection"
  labels:
    {{- include "securing-web-week8.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "securing-web-week8.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
